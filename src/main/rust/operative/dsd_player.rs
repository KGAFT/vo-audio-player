//There must be dsd player, with ability to play dsd without pcm conversion
//But asio is hard af, also needed UAC2 for linux/android

pub struct DsdPlayer{

}

impl DsdPlayer{
    pub fn new() -> Self{
        /*
        let bind = asio_sys::Asio::new();
        let driver = bind.load_driver("FiiO ASIO Driver").expect("Failed to load audio driver!");
        driver.
        Self{}


         */
        Self{}
    }
}